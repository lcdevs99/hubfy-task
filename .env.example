# Chave secreta usada para assinar os tokens JWT
JWT_SECRET=

# Tempo de expiração do token JWT
# Pode ser em segundos (3600), horas (1h), dias (7d)
JWT_EXPIRES_IN=7d

# Porta do servidor (opcional, Next.js usa 3000 por padrão)
PORT=3000

# Ambiente da aplicação
NODE_ENV=development